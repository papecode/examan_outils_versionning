# Service Utilisateurs

Port interne : `8000` (hote par defaut : `8000`).

Endpoints cibles : `POST /users`, `GET /users/check/{identifier}`, `GET /users`, `GET /users/{user_id}`.

Implémentation backend : équipe microservices.
