# Service Emprunts

Port interne : `8000` (hote par defaut : `8003`).

Endpoints cibles : `POST /loans`, `GET /loans/user/{user_id}`, `GET /loans/history`, `GET /loans/export`.

Implémentation backend : équipe microservices.
